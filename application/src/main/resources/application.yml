# server
server:
  port: 18000
  servlet:
    context-path: /

# spring
spring:
  application:
    name: security-oauth2-server
  main:
    allow-circular-references: true
    allow-bean-definition-overriding: true
  profiles:
    active: dev